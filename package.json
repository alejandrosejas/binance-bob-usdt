{
  "name": "binance-bob-usdt",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "proxy-server"
  ],
  "scripts": {
    "start": "cd proxy-server && node server.js",
    "build": "cd proxy-server && npm install"
  }
}
